# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is currently a near-empty scaffold. It contains only `README.md`
(placeholder content) and has no source code, build tooling, dependency manifest,
or tests yet.

As the project takes shape, update this file with:
- Build, lint, and test commands (including how to run a single test)
- High-level architecture that spans multiple files and isn't obvious from the
  directory layout

## Git

The default branch is `main`. Development for the current task happens on branch
`claude/create-claude-md-b32db9`.
